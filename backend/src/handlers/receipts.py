from src.utils.response import error_response


def presign(event):
    """Generate a presigned URL for receipt upload. (stub)"""
    return error_response("Funcionalidade não implementada", 501)


def process(event):
    """Process a receipt image via OCR. (stub)"""
    return error_response("Funcionalidade não implementada", 501)
